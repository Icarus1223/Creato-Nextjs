import { ButtonProps } from "./interface";

export default function SecondaryButton(props: ButtonProps) {
    return (
        <button>
            <span></span>
            <span></span>
        </button>
    )
}